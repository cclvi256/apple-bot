[napcat]
# Must match the token on NapCat's HTTP client (event webhook).
webhook_token = "replace-event-token"
# Must match the token on NapCat's HTTP server (OneBot actions).
api_token = "replace-api-token"
